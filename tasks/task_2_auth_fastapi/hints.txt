HINTS for Auth FastAPI task:

1. SESSION SETUP (required):
   from starlette.middleware.sessions import SessionMiddleware
   app.add_middleware(SessionMiddleware, secret_key="vibe-secret-key-change-me")

2. ACCESS SESSION in route handlers:
   from starlette.requests import Request
   @app.get("/dashboard")
   async def dashboard(request: Request):
       user_email = request.session.get("user_email")
       if not user_email:
           return RedirectResponse("/login")

3. TEMPLATES SETUP:
   from fastapi.templating import Jinja2Templates
   templates = Jinja2Templates(directory="templates")
   return templates.TemplateResponse("dashboard.html", {"request": request, "user": user})

4. PRE-SEEDED DATA (put at module level):
   USERS = {
       "alice@test.com": {
           "name": "Alice Smith",
           "email": "alice@test.com",
           "password": "password123"
       }
   }
   ORDERS = {
       "alice@test.com": [
           {"id": 1, "item": "Laptop", "status": "Delivered", "date": "2024-01-15"},
           {"id": 2, "item": "Mouse", "status": "Processing", "date": "2024-01-20"},
       ]
   }

5. HTML TEMPLATE EXAMPLE (dashboard.html):
   <h1 id="welcome-msg">Welcome, {{ user.name }}!</h1>
   <table id="orders-table">
     {% for order in orders %}
     <tr class="order-row">
       <td>{{ order.id }}</td>
       <td>{{ order.item }}</td>
       <td>{{ order.status }}</td>
     </tr>
     {% endfor %}
   </table>
   <a id="logout-link" href="/logout">Logout</a>

6. REGISTER: Add new users to the USERS dict (in-memory, no DB needed)

7. ERROR DISPLAY on login failure:
   templates/login.html: {% if error %}<div id="login-error">{{ error }}</div>{% endif %}
   Route: return templates.TemplateResponse("login.html", {"request": request, "error": "Invalid credentials"})
